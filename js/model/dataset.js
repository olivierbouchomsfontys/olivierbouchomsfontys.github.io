class Dataset{
    name;
    url;
    dataKey;
    labelKey;

    constructor(name, url, dataKey, labelKey) {
        this.name = name;
        this.url = url;
        this.dataKey = dataKey;
        this.labelKey = labelKey;
    }
}
